<!DOCTYPE html>
<html>

<head>
	<meta http-equiv="content-type" content="text/html;charset=UTF-8" />
	<meta charset="utf-8" />
	<title>Source II.0</title>
	<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />

	<meta name="apple-mobile-web-app-capable" content="yes">
	<meta name="apple-touch-fullscreen" content="yes">
	<meta name="apple-mobile-web-app-status-bar-style" content="default">

	<!-- BEGIN Vendor CSS-->
	<link href="css/BsdtSrc.css" rel="stylesheet" type="text/css" />

	<!--[if lte IE 9]>
	<link href="css/ie9.css" rel="stylesheet" type="text/css"/>
	<![endif]-->

	<script type="text/javascript">
		window.onload = function() {
			// fix for windows 8
			if (navigator.appVersion.indexOf("Windows NT 6.2") != -1)
				document.head.innerHTML += '<link rel="stylesheet" type="text/css" href="css/windows.chrome.fix.css" />'
		}
	</script>

</head>

<body class="fixed-header">

<div class="appbase"></div>
<div class="modal fade slide-up" id="modal-region" role="dialog" aria-hidden="true"></div>

<!-- BEGIN VENDOR JS -->
<script src="js/vendor.js" type="text/javascript"></script>


<script type="text/javascript">

	function readCookie(cname) {
		var name = cname + "=";
		var ca = document.cookie.split(';');
		for(var i=0; i<ca.length; i++) {
			var c = ca[i];
			while (c.charAt(0)==' ') c = c.substring(1);
			if (c.indexOf(name) == 0) return c.substring(name.length,c.length);
		}
		return "";
	}

	$(document).ajaxSend(function(event, request) {
		var token = readCookie('token');
		if (token) {
			request.setRequestHeader('authorization', 'Bearer ' + token);
		}
	});

	// Hack for active drop visibilty after modal is closed
	$(document).click(function(){
		$('.select2-modal').select2('close');
	});

	$.ajaxSetup({ cache: false });
</script>

<!-- MARIONETTE APP -->
<script src="js/app.js" type="text/javascript"></script>

<!-- CORE PLUGINS AND UI -->
<script src="js/BsdtSrc.min.js" type="text/javascript"></script>
<script src="js/kendo.all.min.js" type="text/javascript"></script>

<script>

	$(document).ajaxStop(function() {
		$('[data-pages="sidebar"]').each(function() {
			var $sidebar = $(this);
			$sidebar.sidebar($sidebar.data())
		})
	})
</script>
</body>
</html>